# FinBERT scoring. Installed by the pipeline workflow; optional locally (the model is ~440 MB).
# Install torch from the CPU index first, so pip skips the much larger CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch
transformers>=4.40
